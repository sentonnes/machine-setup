<#
.SYNOPSIS
    STUB — not yet implemented.
    Ideas: shared plugin cache dir via .tofurc / TOFU_CLI_CONFIG_FILE
    (mirrors configure-terraform.ps1, but OpenTofu reads its own config file —
    the two are NOT interchangeable, hence a separate script).
#>

$ErrorActionPreference = 'Stop'
. "$PSScriptRoot\_common.ps1"

Write-Step "Configuring OpenTofu"
Write-Skip "not yet implemented — see TODO in this file"

# TODO
