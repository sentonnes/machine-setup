# machine-setup

Windows dev machine bootstrap scripts.

## Usage

Just run:

```powershell
.\setup.ps1
```

This installs everything (winget packages + PowerShell modules) and configures the machine. It's idempotent, so it's safe to re-run any time to pick up updates.
